import React, { Fragment } from 'react';
import { useDispatch, useSelector } from 'react-redux';

// import { Container } from './styles';

function ButonBonus() {
	return (
		<Fragment>
			<div className={'buton-bonus'}>
				<p>{'RULES'}</p>
			</div>
		</Fragment>
	);
}

export default ButonBonus;
